# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build
/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files
.env
.env*.local
.env.production

# Content saved from /admin (one JSON per key). This is live site data written
# on the server — it must never be committed, and must never be shipped in a
# deploy archive, or an edit made through the admin UI gets overwritten.
/content

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.idea/
.vscode/

# vercel
.vercel
